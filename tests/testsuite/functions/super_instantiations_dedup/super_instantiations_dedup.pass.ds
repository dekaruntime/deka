import { echo } from "io"
struct User {
  id: string;
}
super fn validate<T>(json: string) Type {
  return T.type()
}
const a = validate<User>("{}")
const b = validate<User>("{}")
echo(string(a == b))
