[build-wasm] using local compiler: /Users/sami/Projects/deka-website/public/tour/deka_compiler.wasm (4727665 bytes)
import { echo } from "io"
const app = <Card />
fn Card() Component {
  return <div>hi</div>
}
const result = unsafe { deka.ui.renderToString(app) }
match (result) {
  Ok(r) => echo(r.html),
  Err(e) => echo(e.message),
}
