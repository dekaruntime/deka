import { echo } from "io"

const upper = unsafe<string> { String(42) }

match (upper) {
  Ok(v) => echo(v),
  Err(e) => echo("threw"),
}

const nothing = unsafe<void> { globalThis.__deka_probe = 1 }
match (nothing) {
  Ok(v) => echo("ok"),
  Err(e) => echo("threw"),
}
