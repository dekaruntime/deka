import { constant, first } from "./lib.ds"

const fixed: string = match (constant({sub: "1"})) {
  Ok(value) => value,
  Err(error) => error,
}
const item: string = match (first(["x"])) {
  Some(value) => value,
  None => "",
}

unsafe<void> { console.log(fixed + item) }
