import { echo } from "io"
interface Card {
  title: string
  subtitle?: string
}
fn show(c: Card) {
  if (isset(c.subtitle)) {
    echo(c.subtitle)
  } else {
    echo(c.title)
  }
}
show({ title: "Hello", subtitle: "World" })
show({ title: "Hello" })
