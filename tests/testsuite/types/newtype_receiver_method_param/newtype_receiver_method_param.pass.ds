import { echo } from "io"

type Cents number
fn (c Cents) add(other: Cents) Cents {
  return Cents(unbox_number(c) + unbox_number(other))
}
const a = Cents(100)
const b = Cents(200)
echo(string(unbox_number(a.add(b))))
