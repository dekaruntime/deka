import { echo } from "io"

const good = parse_number("3.14")
match (good) {
  Some(v) => echo(string(v)),
  None => echo("none"),
}
const bad = parse_number("abc")
match (bad) {
  Some(v) => echo(string(v)),
  None => echo("none"),
}
