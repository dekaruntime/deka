{
  "title": "first()/last() return a real Option, including on frozen arrays",
  "stage": "run",
  "notes": "deka#561: the emitter rewrites first()/last() to an Option-producing expression (unlike pop/shift passthroughs). Covers empty arrays, const frozen arrays, literal receivers, and match on the result."
}
