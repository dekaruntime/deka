import { echo } from "io"

enum Colour {
  Red,
  Green,
  Blue,
}

fn warmth(c: Colour) string {
  return match (c) {
    Red | Green => "warm",
    Blue => "cool",
  }
}

echo(warmth(Colour.Red))
echo(warmth(Colour.Green))
echo(warmth(Colour.Blue))
