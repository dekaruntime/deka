struct User {
  name: string
  email: Option<string>
}

const u = User { name: "DekaScript" };

const contact = match (u.email) {
  Some(value) => value,
  None => "no email",
  _ => "unknown",
};

console.log(contact);
