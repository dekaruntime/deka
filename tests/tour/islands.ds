import { echo } from "io"

struct CounterProps {
  start: number
}

fn Counter(props: CounterProps) Component {
  return <button>{props.start}</button>
}

const result = unsafe { deka.ui.renderToString(<Counter client:load start={5} />) }
echo(unsafe { result.html })
