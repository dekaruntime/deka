---
import { echo } from "io"
fn Counter(props: object) Component {
  return <button>{props.start}</button>
}
---
const result = deka.ui.renderToString(<Counter client:load start={5} />)
echo(result.html)
