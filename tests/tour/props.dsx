interface CardProps {
  title: string;
  subtitle?: string;
}

fn Card(props: CardProps) Component {
  const sub = match (props.subtitle) {
    Some(s) => <p>{s}</p>,
    None => <></>,
  }

  return <div>
<h2>{props.title}</h2>
{sub}
</div>
}

<Card title={"Hello"} subtitle={"DekaScript"} />
