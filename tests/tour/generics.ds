import { echo } from "io"

fn first<T>(items: Array<T>, fallback: T) T {
  if (items.length == 0) {
    return fallback
  }

  return items[0]
}

echo(string(first([3, 4, 5], 0)))
echo(first(["a", "b"], "none"))
echo(first([], "empty"))
