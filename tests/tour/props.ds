interface CardProps {
  title: string
  subtitle?: string
}

fn Card(props: CardProps) Component {
  return <div>
    <h2>{props.title}</h2>
    <p>{props.subtitle}</p>
  </div>
}

<Card title="Hello" subtitle="DekaScript" />
