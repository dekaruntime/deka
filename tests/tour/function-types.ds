type Adder = fn(number, number) number

const add = fn(a: number, b: number) number {
  return a + b
}

fn callAdder(f: Adder, a: number, b: number) number {
  return f(a, b)
}

console.log(callAdder(add, 20, 22))
