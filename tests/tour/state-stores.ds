import { echo } from "io"
fn increment(s: object) object {
  return { count: s.count + 1 }
}

fn add(s: object, n: number) object {
  return { count: s.count + n }
}

const useCounter = deka.ui.State.create({ count: 0 }, {
  increment: increment,
  add: add,
})

echo(useCounter().count)
useCounter.increment()
echo(useCounter().count)
useCounter.add(5)
echo(useCounter().count)
