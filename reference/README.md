# Reference material

This directory holds read-only reference copies of older code that is useful while rewriting parts of the compiler. Nothing here is compiled or depended on by the current build.

## Contents

- `phpx-typeck-v1/` — snapshot of the v1 PHPX typechecker copied from `crates/deka_syntax/src/phpx` during the Compiler v2 rewrite. Kept temporarily for porting guidance; delete once v2 typechecking covers the same surface.
