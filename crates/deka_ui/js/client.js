// ui/client — hydrate / island registry. Phase 1 is a stub: walk-and-attach
// lands in phase 9. Safe to import with no document.

export function hydrate() {
  return;
}

export function registerIsland() {
  return;
}
