[package]
name = "deka_ui"
version.workspace = true
edition = "2021"
description = "DekaScript UI runtime modules (jsx / reactive / client / server)"
license = "MIT"

[lib]
path = "src/lib.rs"

[dev-dependencies]
deno_core = { workspace = true }
tempfile = "3.0"
tokio = { workspace = true }
