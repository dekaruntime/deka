import { HelloBadge } from "../src/ui/Hello.dsx"

interface LayoutProps {
  children: ReactNode
}

export fn Layout(props: LayoutProps) ReactNode {
  return <div>
      <header>
        <HelloBadge client:load />
      </header>
      <main>{props.children}</main>
    </div>
}
