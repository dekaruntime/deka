import { jsx } from "@js/react/jsx-runtime"

export fn manualJsx() ReactNode {
  return jsx("p", { children: "manual jsx" })
}

import { jsxs, Fragment } from "@js/react/jsx-runtime"

export fn manualMany() ReactNode {
  return jsxs(Fragment, { children: ["one", "two"] }, 7)
}
