[package]
name = "compile"
version.workspace = true
edition = "2024"

[dependencies]
core = { path = "../core" }
compiler = { path = "../compiler" }
tempfile = "3.23.0"
stdio = "0.4.0"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
flate2 = "1.0"

[dev-dependencies]
tempfile = "3.0"
