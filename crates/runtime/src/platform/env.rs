// Platform database and development settings are parsed from deka.json by the
// caller. This file remains only to preserve the module path during the
// migration; it deliberately has no process-environment seam.
