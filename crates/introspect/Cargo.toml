[package]
name = "introspect"
version.workspace = true
edition = "2021"

[dependencies]
core = { path = "../core" }
runtime = { path = "../runtime" }
stdio = { path = "../stdio" }
serde_json = "1.0"
tempfile = "3.23.0"
