[package]
name = "deka-test"
version.workspace = true
edition = "2024"

[dependencies]
deka-cli-core = { version = "0.5.0", default-features = false, features = ["registry"] }
stdio = "0.4.0"
deka-modules = "0.3.0"
serde_json = "1.0"
deka_host = { path = "../deka_host", default-features = false, features = ["runtime"] }
