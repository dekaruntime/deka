// Explicit function declaration lets 0.50.0 verify the boundary's shape.
import React from '../js_modules/react/index.mjs';
export function createText(text) { return React.createElement('span', {}, text); }
