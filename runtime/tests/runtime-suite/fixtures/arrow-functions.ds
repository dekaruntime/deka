const typed = fn(n: int) int { return n * 2 };
const untyped = fn(n: int) int { return n * 2 };
const add = fn(left: number, right: number) number { return left + right };
console.log(typed(21));
console.log(untyped(21));
console.log(add(20, 22));
