name: smoke-pause-then-resume
duration: 30s
faults:
  - at: 5s
    host: demon
    cmd: process pause --service gg.tana.harar --secs 5 --undo-by 10s
assertions:
  - at: 20s
    kind: systemd
    host: demon
    check: gg.tana.harar is-active
