[package]
name = "tana-secrets"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for reading secrets from tana-vault-agent"

[dependencies]
serde_json = { workspace = true }
tokio = { workspace = true, features = ["io-util", "net", "sync"] }

[dev-dependencies]
tempfile = "3"
