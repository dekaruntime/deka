---
title: "Buffer"
section: "runtime"
category: "types"
categoryLabel: "Types"
description: "A Buffer is a mutable byte buffer with a cursor."
---

# Buffer

`Buffer` is a mutable byte buffer with a cursor. It is defined as:

```phpx
type Buffer = {
  data: string
  cursor: int
}
```
