---
title: "Result"
section: "runtime"
category: "types"
categoryLabel: "Types"
description: "A Result type models either a successful value or an error."
---

# Result

`Result<T, E>` models either a successful value (`Ok<T>`) or an error (`Err<E>`). It is used throughout the runtime for operations that may fail without throwing.
